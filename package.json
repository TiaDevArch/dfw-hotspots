{
  "name": "dfw-hotspot-vibes",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "airtable:pull": "node scripts/pullAirtableData.js",
    "update": "npm run airtable:pull && git add -A && (git diff --cached --quiet && echo \"No changes to commit.\" || git commit -m \"Airtable sync: $(date '+%Y-%m-%d %H:%M')\") && git push"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/netlify": "^6.0.0",
    "astro": "^5.0.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
    "@netlify/vite-plugin": "^1.0.0"
  },
  "devDependencies": {
    "@astrojs/netlify": "^6.5.13"
  }
}
